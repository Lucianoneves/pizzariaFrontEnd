import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

// next.config.js
module.exports = {
  images: {
    qualities: [75, 100], // lista de qualidades permitidas
  },
};


export default nextConfig;
